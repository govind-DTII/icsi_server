# Copy to .env and adjust if needed for local Postgres.
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=dtii
DB_NAME=ascent.en
PORT=3000
JWT_SECRET=local-dev-secret-change-me
# production must be "production" (keeps TypeORM synchronize off)
NODE_ENV=development
# Optional hard-off for synchronize even in non-production:
# DB_SYNCHRONIZE=false
